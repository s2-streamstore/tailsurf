---
"@tailsurf/protocol": minor
"@tailsurf/client": minor
"tailsurf": minor
---

Make `transcript`, `bytes`, and `terminal` immutable stream kinds. Remove per-record formats from REST, SSE, and WebSocket records. Add the stream kind to WebSocket readiness handshakes.

Add terminal event codecs, canonical terminal link builders, and separate terminal input and output WebSocket clients.
