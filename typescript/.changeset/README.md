# Changesets

These files describe pending TypeScript package releases.

Create one from `typescript`:

```sh
pnpm changeset
```

See [TypeScript releases](../RELEASING.md) for the complete workflow.
