# Rust SDK and CLI

This directory contains the public Rust implementation of TSF.

- `tailsurf` is the async SDK and protocol crate.
- `tailsurf-cli` is the `tsf` command-line application.

The repository root owns the Cargo workspace, lockfile, release configuration, and shared development documentation.
